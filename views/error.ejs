<!DOCTYPE html>
<html lang="en">
  <%- include('partials/head.ejs', { pageTitle: (status || 500) + ' Error' }) %>
  <body class="min-h-screen bg-base-100 flex items-center justify-center">
    <div class="max-w-md w-full p-6 bg-white rounded-2xl shadow border border-gray-100">
      <h1 class="text-2xl font-bold mb-2"><%= status || 500 %> Error</h1>
      <p class="text-gray-600"><%= message || 'Something went wrong.' %></p>
      <div class="mt-4 flex gap-2">
        <a href="/" class="btn btn-primary">Back to Dashboard</a>
        <button class="btn" onclick="history.back()">Go Back</button>
      </div>
    </div>
  </body>
</html>
