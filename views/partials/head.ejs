<head>
  <meta charset="utf-8" />
  <title><%= pageTitle || 'AfroVibe' %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link rel="icon" href="/favicon.ico" />
  <link rel="stylesheet" href="/css/tw.css?v=afro2">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">

  <!-- ONLY the compiled stylesheet -->
  <link rel="stylesheet" href="/css/output.css?v=<%= Date.now() %>">

  <!-- No Tailwind CDN. No DaisyUI CDN. Keep things single-source. -->
  <script src="/socket.io/socket.io.js"></script>

  <style>
    /* Common styles for the app */
    .photo-gallery {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
      gap: 1rem;
    }
    .photo-gallery img {
      width: 100%;
      height: 200px;
      object-fit: cover;
      border-radius: 0.5rem;
      transition: transform 0.3s ease;
    }
    .photo-gallery img:hover {
      transform: scale(1.05);
    }
    .photo-placeholder {
      width: 100%;
      height: 200px;
      background-color: #e5e7eb;
      border-radius: 0.5rem;
      display: flex;
      align-items: center;
      justify-content: center;
      color: #6b7280;
    }
  </style>
</head>
